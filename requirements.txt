# Runtime dependencies. Majors are capped so an upstream breaking change
# cannot turn a fresh clone into a broken install.
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
pydantic>=2.7,<3.0
# Food: barcode photos are decoded with zxing-cpp (self-contained wheel, no system
# library) and opened with Pillow; python-multipart parses the upload.
zxing-cpp>=2.2
Pillow>=10.0
python-multipart>=0.0.9
