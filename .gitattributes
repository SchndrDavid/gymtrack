# Normalise to LF in the repository. Authored on Windows, hosted on Linux —
# without this a CRLF line ending can travel into the image and break a script.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.ico binary
*.db binary
